'use client';

import Layout from '@/app/componentes/Layout';
import WhatsappConnect from '@/app/componentes/WhatsappConnect';

export default function ConnectionPage() {
  return (
    <Layout>
      <WhatsappConnect />
    </Layout>
  );
}